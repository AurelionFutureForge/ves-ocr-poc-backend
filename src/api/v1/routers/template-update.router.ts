import { TemplateController } from "@/v1/controllers";
import express from "express";

const templateUpdateRouter = express.Router();

// Template Update Route - PUT endpoint

export default templateUpdateRouter;

